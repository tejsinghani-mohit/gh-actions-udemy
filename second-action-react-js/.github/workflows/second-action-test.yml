name: Second Action Workflow

on:
  push:
    paths:
      - "second-action-react-js/**"  # Trigger only on changes within this project

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Step 2: Navigate to the project folder
      - name: Navigate to second-action-react-js
        working-directory: second-action-react-js
        run: |
          echo "Running workflow for second-action-react-js"
          ls -l  # List files in the project folder

      # # Step 3: Example React.js build step (if required)
      # - name: Install Dependencies
      #   working-directory: second-action-react-js
      #   run: npm install

      # - name: Build React Application
      #   working-directory: second-action-react-js
      #   run: npm run build
